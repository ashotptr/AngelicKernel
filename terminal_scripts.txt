1) installing tools
sudo apt install build-essential qemu-system-x86 nasm gnu-efi ovmf gdb qemu-utils python3-venv python3-full maven tsung docker.io
python3 -m venv .venv
source .venv/bin/activate
pip install colorama pyopenssl slixmpp

2) installing lwip
1. https://savannah.nongnu.org/projects/lwip/
2. https://download.savannah.nongnu.org/releases/lwip/
3. https://git.savannah.gnu.org/git/lwip/lwip-contrib.git/
4. git clone git://git.savannah.gnu.org/lwip.git
5. git clone https://github.com/lwip-tcpip/lwip.git // the one I used to use
6. git clone git://git.savannah.nongnu.org/lwip.git // the one I use

3) making exectable
chmod +x run.sh

4) checking abi
objdump -d src/drivers/e1000.o | grep -A 20 "<e1000_send_raw>:"

5) debugging
gdb -ex "target remote localhost:1234" -ex "symbol-file unikernel.debug"

6) repo into .txt file
npx repomix --remote 'https://github.com/ashotptr/AngelicKernel' --style plain
npx repomix --remote https://github.com/ashotptr/AngelicKernel --style plain --ignore "mbedtls/,src/lwip/,testing/tigase-tts-ng/"

7) one time mbedtls installation script
make mbedtls-fetch
